<?xml version="1.0" encoding="utf-8"?>
<!-- EJEMPLO DE CONFIGURACIÓN ANDROIDMANIFEST.XML PARA NFC INTENT LAUNCHING -->
<!-- 
Este archivo muestra las configuraciones necesarias para que tu app Flutter HCE
pueda ser lanzada automáticamente cuando se reciban comandos NFC con AIDs específicos.

INSTRUCCIONES DE USO:
1. Copia las secciones <activity> y <service> relevantes
2. Pégalas en tu archivo android/app/src/main/AndroidManifest.xml
3. Modifica los AIDs según tus necesidades específicas
4. Asegúrate de tener los permisos NFC correctos
-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- PERMISOS NFC NECESARIOS -->
    <uses-permission android:name="android.permission.NFC" />
    
    <!-- FEATURE NFC REQUERIDO -->
    <uses-feature
        android:name="android.hardware.nfc.hce"
        android:required="true" />

    <application
        android:name="${applicationName}"
        android:exported="false"
        android:icon="@mipmap/ic_launcher"
        android:label="flutter_hce_example">
        
        <!-- ACTIVIDAD PRINCIPAL CON INTENTS NFC -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme">
            
            <!-- INTENT FILTER ESTÁNDAR PARA LANZAR LA APP -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            
            <!-- INTENT FILTER PARA NFC TECH DISCOVERY -->
            <!-- Se activa cuando se detecta tecnología NFC específica -->
            <intent-filter>
                <action android:name="android.nfc.action.TECH_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            
            <!-- INTENT FILTER PARA NDEF DISCOVERY -->
            <!-- Se activa cuando se detectan tags NDEF -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            
            <!-- INTENT FILTER PARA TAG DISCOVERY (más general) -->
            <intent-filter>
                <action android:name="android.nfc.action.TAG_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- TECNOLOGÍAS NFC SOPORTADAS -->
            <meta-data
                android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/nfc_tech_filter" />

        </activity>

        <!-- SERVICIO HCE PARA HOST CARD EMULATION -->
        <service
            android:name="io.flutter.plugins.nfc_host_card_emulation.HceService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            
            <!-- CONFIGURACIÓN DEL SERVICIO HCE -->
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/apdu_service" />
        </service>

        <!-- METADATOS FLUTTER (YA EXISTENTES) -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- QUERIES PARA COMPATIBILIDAD (SI ES NECESARIO) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
